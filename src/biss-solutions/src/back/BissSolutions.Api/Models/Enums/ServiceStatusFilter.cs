namespace BissSolutions.Api.Models.Enums
{
    public enum ServiceStatusFilter
    {
        Active = 0,     // Apenas ativos
        Inactive = 1,   // Apenas inativos
        All = 2         // Todos (ativos e inativos)
    }
}

